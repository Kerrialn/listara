'use client'
import React from 'react'

const ProductCart = () => {
    return (
        <div>
            <button onClick={() => {
                console.log('clicked!!!!')
            }}>Add To Cart
            </button>
        </div>
    );
}

export default ProductCart
